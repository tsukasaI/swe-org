問題	
1	動的配列と LinkedList の使い分けは?


2	Stack と Queue はどのように使い分ける?


3	Open Hash, Close Hash の違いは?


4	2分探索、クイックソート、挿入ソートの計算量は?


5	ソートが安定的であるとは?



6	ハッシュマップや2分木の検索・挿入の計算量は? なぜハッシュマップは早い?



7	バケットソートの計算量は? なぜクイックソートより早い?


8	O(n), O(n^2), O(n^3), O(2^n), O(n log(n)) のアルゴリズムで、 n が2倍になると速度はどうなる?


9	巡回セールスマン問題について説明せよ


10	貪欲法・分割統治法について説明し、代表的なアルゴリズムを１つずつ挙げよ



11	深さ優先探索・幅優先探索とは? どう使い分ける?


12	深さ優先探索を再帰を使わずにループで書くにはどうすれば良いか?


13	"以下のアルゴリズムの時間計算量は?
for (int i = 0; i < n; i++) {
  for (int j = 0; j <n; j++) {
    data[i][j] = 0;
    for (int k=0; k<n; k++) {
      data[i][j] += data[i][k] * data[k][j];
    }
  }
}"


14	"以下のアルゴリズムの時間計算量は?
func(n, x): return func(0, n, x)
func(a, b, x):
  if (b-a == 1) return (data[a] == x)
  m = (b-a)/2
  if (data[m] == 0) return true
  if (res > 0) return func(a, m, x) 
  return func(m+1, b, x)"


15	3SAT とはどういう問題か?
16	NP 完全な問題は現実的な時間で解けるのか?
17	NP 完全問題を計算する時にはどうアプローチするか?
18	正則言語・文脈自由言語・帰納的可算言語に対応するオートマトンは?
19	正則言語・文脈自由言語だが正則でない言語・帰納的可算言語だが文脈自由言語出ない言語の例を挙げよ
20	一般にプログラムが停止するか判定するプログラムは存在するか？正則言語・文脈自由言語のプログラムは停止判定可能か?
21	あなたが提供するプログラムの設定言語を分析すると、チューリング完全であることがわかった。その際、何に気を付けるべき点はあるか?
